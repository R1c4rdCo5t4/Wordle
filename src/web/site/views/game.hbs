<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Icons+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/styles.css">
  <title>Wordle</title>
</head>
<body>
  <div class="word">
    <div class="word-header">
      <h1>Wordle</h1>
    </div>
    <div class="word-stats">
        <div class="guesses">
            Number of guesses: {{guesses}}
        </div>
    </div>
    <div class="word-container">
      <div class="guess-letters">
        {{#each guessedWords}}
          <div class="guess-letters-container" id={{@index}}>
            {{#each this}}
              <a class="letters-item" style="color:{{this}}">{{@key}}</a>
            {{/each}}
          </div>
        {{/each}}
      </div>
    </div>
    <div class="word-guess-area">
      <form action="/play" method="GET">
          <input type="submit" value="New Game" id="button">
      </form>
      <form action="/play/{{id}}" method="POST">
        <label for="word-guess">
          <input type="text" placeholder="..." name="guess" id="textBox">
          <input type="submit" value="Try" id="button">
        </label>
      </form>
    </div>
  </div>
  <script>
    
  </script>
</body>
</html>